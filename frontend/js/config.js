/**
 * MediCode AI - Configuration
 * Contains application-wide configuration settings
 */

const Config = {
    // API endpoint URL - change this to point to your backend server
    apiUrl: 'http://localhost:3000',
    
    // Default settings
    defaultTheme: 'light',
    
    // Version
    version: '1.0.0'
}; 